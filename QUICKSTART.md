# 🚀 Быстрый деплой - 5 минут

## Шаг 1: Telegram Bot (2 мин)

1. Открой [@BotFather](https://t.me/botfather)
2. Отправь: `/newbot`
3. Скопируй токен (например: `5234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`)

## Шаг 2: Telegram Channel (1 мин)

1. Создай канал (если нет)
2. Добавь бота как админа (права на постинг)
3. Узнай ID канала:
   - Публичный: используй `@channel_name`
   - Приватный: перешли пост в [@username_to_id_bot](https://t.me/username_to_id_bot)
   - ID: `-1001234567890`

## Шаг 3: GitHub Setup (2 мин)

### Вариант A: Fork репозитория
```bash
# 1. Fork на GitHub
# 2. Clone локально
git clone https://github.com/твой-username/crypto-news-bot
cd crypto-news-bot
```

### Вариант B: Создать с нуля
```bash
# 1. Создай новый репо на GitHub
# 2. Clone и добавь файлы
git clone https://github.com/твой-username/твой-репо
cd твой-репо

# Скопируй все файлы из crypto-news-bot
cp -r /path/to/crypto-news-bot/* .

git add .
git commit -m "Initial crypto news bot setup"
git push
```

## Шаг 4: GitHub Secrets

1. Открой репозиторий на GitHub
2. Settings → Secrets and variables → Actions → New repository secret

Добавь **2 секрета**:

| Name | Value | Пример |
|------|-------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен от BotFather | `5234567890:AAHdqTc...` |
| `TELEGRAM_CHANNEL_ID` | ID канала | `-1001234567890` или `@channel` |

## Шаг 5: Первый запуск

1. Actions → Enable workflows (если нужно)
2. Crypto News Bot → Run workflow → Run
3. Жди ~1 минуту
4. Проверь канал! 🎉

## ✅ Готово!

Теперь бот работает автоматически каждые 4 часа.

---

## 🔧 Что дальше?

### Настрой под себя

1. **Чаще/реже публикации**: `.github/workflows/crypto_news.yml` → измени cron
2. **Больше/меньше новостей**: `news_config.py` → измени `MIN_IMPORTANCE_SCORE`
3. **Другие темы**: `news_config.py` → добавь ключевые слова

### Примеры настроек

**Только критические события:**
```python
# news_config.py
MIN_IMPORTANCE_SCORE = 80
```

**Каждые 2 часа:**
```yaml
# .github/workflows/crypto_news.yml
cron: '0 */2 * * *'
```

**Фокус на регуляции:**
```python
# news_config.py - добавь в IMPORTANCE_RULES
'REGULATORY': {
    'weight': 150,
    'keywords': ['sec', 'regulation', 'law', 'bill', 'congress']
}
```

---

## 🐛 Не работает?

### Проверь логи
Actions → последний запуск → смотри вывод

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| "Unauthorized" | Проверь токен бота в Secrets |
| "Chat not found" | Проверь ID канала, бот должен быть админом |
| "No news published" | Нормально! Значит нет важных новостей за последние 4 часа |

### Тест локально

```bash
export TELEGRAM_BOT_TOKEN="твой_токен"
export TELEGRAM_CHANNEL_ID="твой_id"
python news_parser.py
```

---

## 📊 Мониторинг

Через день проверь Actions:
- Сколько раз запускался (6 раз в сутки = норма)
- Сколько новостей публиковалось
- Если 0 новостей постоянно → снизь `MIN_IMPORTANCE_SCORE`

**Совет:** Первые 2-3 дня смотри, что публикуется, потом подстрой фильтры.
